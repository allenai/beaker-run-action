version: v2
description: Hello, World!
tasks:
  - name: hello
    image:
      docker: hello-world
    result:
      path: /unused
    context:
      preemptible: true
  - name: world
    image:
      docker: hello-world
    result:
      path: /unused
    context:
      preemptible: true
